import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'front.mobile.partenon',
  appName: 'Partenon',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
